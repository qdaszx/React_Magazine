import React from "react";
import styled from "styled-components";

const Text = (props) => {
  const {} = props;
  return (
    <React.Fragment>
      <div></div>
    </React.Fragment>
  );
};

Text.defaultProps = {};

export default Text;
